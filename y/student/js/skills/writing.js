/* ============================================================
   SKILL — Writing (empty shell)
   ============================================================ */

(function () {
  'use strict';
  function renderWritingScreen() { /* no-op placeholder */ }
  window.renderWritingScreen = renderWritingScreen;
})();
