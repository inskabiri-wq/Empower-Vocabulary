/* ============================================================
   SKILL — Speaking (empty shell)
   ============================================================ */

(function () {
  'use strict';
  function renderSpeakingScreen() { /* no-op placeholder */ }
  window.renderSpeakingScreen = renderSpeakingScreen;
})();
